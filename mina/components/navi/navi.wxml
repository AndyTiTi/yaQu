<view class="container">
    <image bind:tap="onLeft" class="navi-icon" src="{{latest?disLeftSrc:leftSrc}}" />
    <text class="title">{{title}}</text>
    <image bind:tap="onRight" class="navi-icon" src="{{first?disRightSrc:rightSrc}}" />
</view>